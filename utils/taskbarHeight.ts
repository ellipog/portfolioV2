/**
 * Authentic Windows XP taskbar height (30px).
 *
 * This is the JS-side source of truth for the taskbar height. The CSS-side
 * source of truth is the `--taskbar-height` custom property in tailwind.css —
 * keep them in sync.
 */
export const TASKBAR_HEIGHT = 40;
